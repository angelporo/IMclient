import AlertMenuBox from './AlertMenuBox.js';
import AlertRedPackage from './AlertRedPackage.js';

export default class AlertBox {
  static AlertMenuBox = AlertMenuBox;
  static AlertRedPackage = AlertRedPackage;
}
